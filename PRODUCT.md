# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

기본 사용자: 네이버·다음 같은 국내 메일 서비스와 Gmail 같은 해외 메일을 함께 쓰는 개인. 처음엔 만든 사람 본인만 쓰는 개인 도구로 시작했고, 지금은 같은 상황(국내+해외 메일 계정을 동시에 쓰는 개인)에 있는 다른 사람도 계정을 만들어 쓸 수 있게 여는 방향으로 진행 중 — 팀/조직용 협업 도구가 아니라 여전히 "개인용 도구"의 연장선.

## Product Purpose

여러 이메일 계정(Gmail·네이버·다음·범용 IMAP)에 흩어진 메일을 한 화면에서 통합해서 보고 관리한다. 계정을 하나하나 따로 열어봐야 하는 불편을 없애는 것이 목적이며, 성공 기준은 "여러 계정 중 하나가 일시적으로 문제가 생겨도 나머지는 정상적으로, 매일 믿고 쓸 수 있는 통합 받은편지함".

## Positioning

Spark, Unibox 같은 상용 통합 메일함 서비스들은 네이버·다음 같은 한국 IMAP 서비스를 지원하지 않는다 — 상업적으로 그럴 유인이 없기 때문. MailRoost는 만든 사람이 실제로 국내+해외 메일을 같이 써야 해서 직접 만든 제품이라, 이 조합(네이버/다음 + Gmail)을 실제로 통합해서 보여주는 몇 안 되는(사실상 유일한) 도구라는 점이 핵심 차별점이다. (자체 보안 설계를 차별점으로 내세우진 않는다 — 자격증명 암호화는 서비스 운영의 기본 책무일 뿐, 전담 보안팀이 있는 대형 서비스 대비 우위라고 보기는 어렵다는 점을 인지하고 있다.)

## Operating Context

하루 단위로 여러 계정의 메일을 확인·정리하는 개인 이메일 트리아지 워크플로: 통합 받은편지함 확인, 읽음/별표/보관, 자동분류 규칙으로 특정 발신자·제목 메일 자동 정리, 스누즈(나중에 다시 보기)·뮤트(특정 발신자 숨기기), 답장·전달·새 메일 작성(서식 있는 HTML 에디터, 첨부파일, 빠른 답장 템플릿), 계정 전체 통합 검색, 첨부파일만 모아보는 첨부함. PWA로 설치해 데스크톱/모바일에서 쓰고, 새 메일 도착 시 브라우저 푸시 알림을 받는다.

## Capabilities and Constraints

- 1인 개발·운영 프로젝트 (전담 팀 없음). Cloudflare Workers 무료 티어 안에서 운영 비용 없이 돌아가는 것이 중요한 제약 — 확장성보다 개인이 감당 가능한 운영비를 우선한다.
- Gmail은 OAuth2 기반 API로, 네이버·다음·범용 메일은 IMAP으로 연동. Cloudflare Workers는 요청마다 상태가 초기화되는 서버리스 환경이라, IMAP 연결도 요청마다 새로 맺는다.
- 국내 IMAP 서버(특히 네이버)는 동시 연결 수 제한 등으로 계정 로드가 일시적으로 실패하는 경우가 있고, 복구까지 몇 분~몇 십분이 걸릴 수 있다 — 알려진 운영상 제약이며 현재 구조적 개선을 진행 중.
- 로그인은 Google OAuth 기반 세션 — 구조적으로는 이미 여러 사용자를 지원하지만(사용자별 세션·상태 분리), 지금까지는 사실상 개발자 본인만 실사용. 다른 사용자에게 열기로 한 만큼, 앞으로는 "사실상 단일 사용자"라는 가정을 코드/설계에 깔지 않는다.
- 계정 자격증명(비밀번호·OAuth 토큰)은 AES-GCM으로 암호화해 저장한다 — 이는 서비스 운영의 기본 책무이지, 내세울 차별점은 아니다(Positioning 참고).

## Brand Commitments

- 이름 "MailRoost" — roost(새들이 둥지로 돌아와 깃드는 곳)에서 따온, 메일이 새처럼 여러 곳에서 한 둥지로 모여든다는 은유.
- 기존 브랜드 요소로 유지: 새/둥지 테마의 빈 상태 일러스트(`frontend/public/mail-empty-roost.png`, "새들이 메일을 나르는 새집")와 손글씨 화살표 + "왼쪽에서 메일을 선택해보세요!" 문구, PWA manifest의 주황색 테마 컬러(#EA580C).

## Evidence on Hand

- 실제로 동작하는 애플리케이션 코드베이스가 유일한 실제 증거다 (frontend/, backend/).
- `MailRoost_Portfolio.md`에 실린 화면 스크린샷은 문서 자체에 "실제 제품의 디자인 시스템과 기능 구성을 바탕으로 제작한 포트폴리오용 목업"이라고 명시된 목업이다 — 실제 프로덕션 스크린샷으로 취급하지 않는다.
- PWA 아이콘, OG 이미지(`frontend/public/og-image.png`) 등은 `frontend/public/`에 실재한다.

## Product Principles

1. 통합은 절반만 되면 의미가 없다 — 지원하는 모든 제공자(특히 네이버/다음 같은 국내 IMAP)가 실제로 안정적으로 동작해야 제품의 존재 이유가 성립한다.
2. 매일 쓰는 개인 트리아지 도구로서 속도와 신뢰성이 화려한 기능보다 우선한다. 계정 하나가 실패해도 나머지 계정 결과는 정상적으로 보여준다.
3. 인프라 비용은 최소로 유지한다 — 서버리스 무료 티어 안에서 운영 가능한 선택을 우선한다.
4. 조용히 실패해서 빈 값을 돌려주는 코드보다, 실패 원인이 로그에 드러나는 코드를 지향한다.
5. 다른 개인 사용자에게 열더라도 팀/조직용 협업 기능으로는 확장하지 않는다 — 여전히 "가벼운 개인 도구"의 정체성을 유지한다.
